# Plotting the Phase Manifold (src/visualize/)

The files in the isualize/ directory (plot_core_physics.py and plot_3d_interactive.py) are used to create the baseline charts of the dataset. 

I used matplotlib and Plotly to graph the generated M-R and M-$\Lambda$ curves so we could visually verify that the physics generation worked perfectly before feeding it into the ML models.
