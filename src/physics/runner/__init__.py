"""Internal, facade-independent implementation leaves for pair experiments.

This package deliberately performs no eager imports; the supported facade
composes its leaf modules in :mod:`src.physics.experiment_runner`.
"""
